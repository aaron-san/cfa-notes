// import fs from "fs";
// import path from "path";

// const files = fs.readdirSync(path.join("app/markdown"));
// const params = files.map((filename) => ({
//   slug: filename.split("-").slice(1).join("-").replace(".mdx", ""),
// }));

// console.log(params);
