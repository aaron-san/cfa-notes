<footer>
  <div class="flex justify-between mx-auto max-w-2xl my-6">
    <% if(locals.frontmatter) { %>
    <button
      class="bg-slate-800 hover:opacity-90 text-slate-200 shadow-sm hover:shadow-white border border-slate-200 px-4 py-2 rounded-md"
    >
      <a href="<%= frontmatter.back_link %>"> Back </a>
    </button>
    <% } %> <% if(locals.frontmatter) { %>
    <button
      class="bg-slate-800 hover:opacity-90 text-slate-200 shadow-sm hover:shadow-white border border-slate-200 px-4 py-2 rounded-md"
    >
      <a href="<%= frontmatter.forward_link %>"> Next </a>
      <% } %>
    </button>
  </div>
  <h4>&copy; <%= new Date().getFullYear(); %></h4>
</footer>
